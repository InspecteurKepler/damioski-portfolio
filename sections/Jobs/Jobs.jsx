import React from 'react';
import './Jobs.scss';

const Jobs = () => {
    return (
        <div className="jobs-section">
            {/* Contenu de la section Jobs */}
        </div>
    );
};

export default Jobs; 